[project]
name = "quiver-to-image"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.10"
dependencies = [
    "pyperclip>=1.11.0",
    "typer>=0.27.2",
]

[project.scripts]
quiver-to-image = "quiver_to_image.cli:app"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
